{% extends modal == '0'
    ? 'base.html.twig'
    : 'trick/empty.html.twig' %}

{% block body %}
    <div class="container">

        {{ include('trick/_edit_form.html.twig', {'button_label': 'Update'}) }}


        <div class="row">
            <div class="col-12 flex-container marginTop30" style="justify-content: flex-end;">
                {{ include('trick/_delete_form.html.twig', {deleteButton: 'yes'}) }}
            </div>
        </div>


        <!-- End DIV for _edit_form -->
    </div>
    </div>
    <!-- End DIV for _edit_form -->



    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {% if modal == 3445 %}
        {{ include('trick/form.ajax.html.twig', {submitPath: path('trick_edit', {'id': form.vars.data.id, 'modal':1})}) }}
    {% endif %}
    {{ include('medias/_delete_media_ajax.html.twig') }}
    <!-- Form Control script -->
    <script src="{{ asset('js/trickForm.js') }}"></script>

{% endblock %}